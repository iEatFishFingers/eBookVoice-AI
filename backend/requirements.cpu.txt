# CPU-optimized requirements for free hosting
# Core Flask dependencies
Flask==2.3.3
Flask-CORS==4.0.0
Werkzeug==2.3.7

# File processing libraries
PyPDF2==3.0.1
ebooklib==0.18.1
beautifulsoup4==4.12.2

# Lightweight TTS for CPU
pyttsx3==2.90
# TTS==0.22.0  # Comment out for lighter install

# Cloud storage dependencies (lightweight)
boto3==1.34.0
google-cloud-storage==2.10.0

# Security and authentication
Flask-JWT-Extended==4.6.0
cryptography==41.0.7
bcrypt==4.1.2

# Additional utilities
python-dotenv==1.0.0
gunicorn==21.2.0

# CPU-only PyTorch (much smaller)
torch==2.1.0+cpu --extra-index-url https://download.pytorch.org/whl/cpu
torchaudio==2.1.0+cpu --extra-index-url https://download.pytorch.org/whl/cpu

# Monitoring and logging (minimal)
structlog==23.2.0

# Redis for free tiers that support it
redis==5.0.1