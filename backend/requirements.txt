# Core Flask dependencies
Flask==2.3.3
Flask-CORS==4.0.0
gunicorn==21.2.0

# Authentication
PyJWT==2.8.0
bcrypt==4.1.2

# File processing - PDF only (EPUB will use built-in libraries)
PyPDF2==3.0.1

# Coqui XTTS v2 for high-quality TTS
TTS==0.22.0
torch>=1.13.0
torchaudio>=0.13.0

# Utilities
requests==2.31.0
python-dotenv==1.0.0